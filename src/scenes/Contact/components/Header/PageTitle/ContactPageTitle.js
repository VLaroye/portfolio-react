import styled from 'styled-components';

const ContactPageTitle = styled.div`
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0,0.8);
    position: absolute;
    top: 0;
    left: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    
        h2 {
            font-family: 'Anton';
            letter-spacing: 0.1em;
            font-size: 3.5em;
            margin: 0;
            padding: 0;
            color: white;
        }
`;

export default ContactPageTitle;